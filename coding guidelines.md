✅ LOW TIER (Easy points — you should get ALL of these)

You’ll naturally hit these just by building the basics:

✔️ Variables + loops
→ Loop through daily checklist items
✔️ Basic conditionals
→ “If task completed → mark done”
✔️ Lists / arrays
→ Store checklist items
✔️ Basic data storage
→ Store streak, completion status
✔️ Data validation (simple)
→ Login form (email/password format)
✔️ Form validation
→ Sign-up page requires all fields
✔️ Sensible output
→ “You completed all tasks today 🎉”

✅ You should easily get full Low points

⚙️ MEDIUM TIER (This is where you separate from average projects)

You already have the foundation—just implement it intentionally:

🔹 Functions that take/return parameters
calculateStreak(userData)
generateDailyTasks(tierLevel)
🔹 Strong data validation
Email format check
Password requirements
Date validation (calendar inputs)
🔹 Multi-variable loops
Loop through:
tasks
AND user progress
AND dates
🔹 User-defined functions
markTaskComplete()
sendReminderNotification()
🔹 Complex conditionals (IMPORTANT)

Example:

If user is Advanced tier AND streak > 30 days → unlock reward
🔹 Frontend + Backend connection

This is BIG for judges.

Example:

Frontend: React / HTML
Backend: Python / Node
Data flows between them
🔹 Classes / objects

Create a User class:

User:
- name
- streak
- tier
- tasksCompleted
🔹 String operations
Parsing user input
Formatting dates
Handling usernames
🔹 Abstraction
Functions for repeated logic
Task generator system
🔹 Data stored across files
User data file
Task system file
Calendar file
🔹 Graphical output
Progress bar
Streak counter
Calendar UI

✅ If you implement most of these → near full Medium points

🚀 HIGH TIER (THIS is how you win)

You do NOT need all of these—but hitting even a few is huge.

⭐ 1. Advanced Data Validation (DO THIS)

Example:

Password must:
Have uppercase
Have number
Have special character
Username rules
Task completion validation
⭐ 2. Date logic (VERY RELEVANT TO YOUR APP)

Perfect for you:

Omer counting:
Only allow correct day
Holiday system:
Must match real calendar
Streak:
Only counts if done within the correct day
⭐ 3. Data Structures

Add one simple but impressive structure:

Dictionary:

tasks = {
  "Easy": [...],
  "Medium": [...],
  "Advanced": [...]
}
OR leaderboard sorting algorithm
⭐ 4. Algorithms

Examples:

Streak calculation algorithm
Task generation algorithm
Ranking friends by streak
⭐ 5. Classes with inheritance (VERY IMPRESSIVE)
BaseUser
   ↓
EasyUser
MediumUser
AdvancedUser

Each has different task loads

⭐ 6. Database / API (HUGE POINT BOOST)

If you do ONE high-level thing—do this.

Options:

Store users in database (Firebase, etc.)
OR use API for:
Zmanim (prayer times)
Jewish calendar
⭐ 7. Derived data
“You completed 85% of tasks this week”
“Your longest streak is 42 days”
⭐ 8. Sets / Dictionaries / Sorting
Sort leaderboard by streak
Store unique users
⭐ 9. Graphical Interface (CRITICAL)

Make it look like a real app:

Dashboard
Checklist UI
Calendar view
Profile page
🧠 STRATEGY TO MAX SCORE
🥇 MUST-HAVE (do these no matter what)
Frontend + backend
Classes + functions
Graphical UI
Calendar system
Streak logic
🥈 HIGH-IMPACT (choose 2–3)
API (zmanim or holidays)
Database (user accounts)
Leaderboard algorithm
Inheritance
🥉 BONUS (if time)
Reflection feature
Notifications
Advanced analytics
🏁 Final Judge Impression Strategy

When presenting, say:

“Our app doesn’t just track tasks—it uses structured data models, algorithmic streak tracking, real-time calendar integration, and scalable backend systems to create a meaningful and technically robust solution.”

That line alone signals HIGH complexity.

🔥 Final verdict

Your idea is:

✅ Conceptually strong
✅ Easy to expand technically
✅ PERFECT for this rubric

If you execute this right, you can absolutely score at the top level.